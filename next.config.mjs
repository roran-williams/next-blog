/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "localhost",
      "source.unsplash.com",
      "scrftc-8000.csb.app",
    ],
  },
};

export default nextConfig;
